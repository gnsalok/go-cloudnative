apiVersion: v1
kind: Service
metadata:
  name: mtls-demo-headless
  namespace: stateful-mtls
spec:
  clusterIP: None
  selector:
    app: mtls-demo
  ports:
    - name: tls
      port: 8443
      targetPort: 8443
---
apiVersion: v1
kind: Service
metadata:
  name: mtls-demo
  namespace: stateful-mtls
spec:
  selector:
    app: mtls-demo
  ports:
    - name: tls
      port: 8443
      targetPort: 8443
